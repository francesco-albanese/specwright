{
  "name": "override-claude-fixture",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "should-be-ignored",
    "lint": "should-be-ignored",
    "format": "should-be-ignored",
    "typecheck": "should-be-ignored",
    "build": "should-be-ignored"
  }
}
