{
  "name": "override-makefile-fixture",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p ."
  }
}
