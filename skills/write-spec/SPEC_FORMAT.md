# Spec: {Spec name}

## Problem Statement

The problem that the user is facing, from the user's perspective.

## Solution

The solution to the problem, from the user's perspective.

## User Stories

A LONG, numbered list of user stories, independently testable. Link this section to the `user-stories.md`.
Stories describe observable behaviour, not mechanism.

## Acceptance criteria (Given/When/Then)

Link this section to the `acceptance-criteria.md`

## System behaviour

System behaviour should be derived from `contracts.md` and `data-model.md`, should include functional/
non-functional requirements, and test intent behaviour (only test external behavior, not implementation
details) with happy paths and failure paths. Each section should be organised in markdown tables for
maximum legibility.

## Out of Scope

A description of the things that are out of scope for this spec.